import React from 'react';

const Update = () => {
  return (
    <div className="update">
      <p>{`Portfolio non terminé, merci pour votre indulgence 😊`}</p>
      <p>{`(Desktop only, 1920 x 1080 px, le responsive arrive bientôt)`}</p>
    </div>
  );
};

export default Update;
